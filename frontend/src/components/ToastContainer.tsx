/**
 * Toast notification container — stub.
 * Full implementation in Task 10.
 */
export function ToastContainer(): React.JSX.Element {
  return (
    <div
      className="fixed top-4 right-4 z-50 space-y-2 pointer-events-none"
      aria-live="polite"
      aria-label="Notifications"
    />
  );
}
